# Afrimercato x Sora – Complete Prompt Pack

Sora generates short clips (10-30 seconds each). Each clip below is one Sora generation.
Stitch them together in CapCut or any editor, then layer the voiceover from 05-AI-PROMPTS.md.

---

## HOW SORA WORKS (before you start)

- Each prompt = one clip (max ~30 seconds)
- Be specific about UI layout, colors, text on screen
- Use phrases like "screen recording style", "app UI mockup", "animated walkthrough"
- You will need 15-20 clips per video, then stitch in an editor
- Add voiceover and captions after stitching

---

## VENDOR VIDEO – 20 SORA PROMPTS

### CLIP 01 – Intro Logo
```
The Afrimercato logo animates into the center of the screen with a burst of green and gold particles. The word "AFRIMERCATO" fades in beneath it in bold white text against a dark green background. Subtle confetti falls. Clean, modern, professional. 10 seconds.
```

### CLIP 02 – Welcome Screen
```
A clean animated app interface appears showing the text "Welcome to Afrimercato" in large green text. Behind it, a blurred vendor dashboard with colorful stat cards fades in softly. The subtitle "Your Vendor Quick Start Guide" slides up beneath. Light, professional tone. 8 seconds.
```

### CLIP 03 – Registration Page
```
A white mobile or desktop registration form appears on screen in a smooth animated mockup style. Fields labeled "Email", "Password", and "Store Name" are visible. A green animated cursor moves down and clicks each field one by one, and text types itself into each box. A green "Register" button glows at the bottom. App UI walkthrough style. 12 seconds.
```

### CLIP 04 – Store Profile Setup
```
A store profile setup form animates onto screen. Fields fill in one by one: "Store Name" fills with "Fresh Farm Foods", a "Category" dropdown opens and selects "Fresh Produce", a description field types itself. Two image upload boxes appear – a logo and a cover image – both fill with colorful placeholder product images. Clean UI mockup style. 15 seconds.
```

### CLIP 05 – Approval Pending
```
A centered screen shows a large animated clock icon ticking gently. Below it, the text "Application Under Review" appears in dark gray, then "24-48 hours" in green fades in beneath. A small envelope icon with a checkmark animates in at the bottom with the label "You'll be notified by email". Calm, reassuring mood. 8 seconds.
```

### CLIP 06 – Vendor Dashboard Overview
```
A full vendor dashboard layout animates in. A sidebar on the left with icons and labels. The main area shows a greeting "Good Morning, Sarah!" at the top. Four colorful KPI cards slide in: Performance Score (92%), Revenue (£5.2K), Orders (156), Customers (89). Each card has a subtle green or gold accent. Clean, modern UI mockup style. 12 seconds.
```

### CLIP 07 – Sidebar Navigation Highlight
```
A dark green vertical sidebar is shown on the left side of the screen. Menu items appear one by one with icons: a chart icon labeled "Dashboard", a box icon labeled "Products", a cart icon labeled "Orders", a graph icon labeled "Reports", a gear icon labeled "Settings". Each item glows green as it appears, then fades to white. A tooltip callout pops up beside each one. Animated UI walkthrough. 12 seconds.
```

### CLIP 08 – Dashboard Charts
```
Three charts animate and draw themselves on a white dashboard background. A green line chart rises showing "Revenue Over Time". A colorful bar chart shows "Orders by Status". A pie chart spins into place showing product categories in green, gold, and blue slices. A small pill-shaped filter bar at the top shows "7d | 30d | 90d | 1y". Data visualization animation style. 10 seconds.
```

### CLIP 09 – Products Page Empty State
```
A clean products page loads. The header says "Products" on the left. A bright green button labeled "+ Add Product" sits in the top right corner. The center of the page shows an empty state illustration – a simple outlined box icon with the text "No products yet. Click Add Product to get started." A green animated arrow bounces and points at the Add Product button. UI walkthrough style. 10 seconds.
```

### CLIP 10 – Product Form Part 1
```
A white modal card slides up from the bottom of the screen. The title "Add New Product" appears in dark text. A text field labeled "Product Name" has a green cursor blinking, then the text "Fresh Organic Tomatoes" types itself in. A "Description" textarea fills next. A "Category" dropdown opens showing options like Fresh Produce, Dairy, Bakery – and "Fresh Produce" is selected with a click. Smooth, animated UI style. 14 seconds.
```

### CLIP 11 – Product Form Part 2
```
Continuing the product form modal. A "Price" field fills with "£3.99". A "Unit" dropdown shows "per kg" selected. A "Stock" field fills with "50". Below, an image upload area shows three tomato product photos sliding in one by one with a gentle pop animation. A green "Create Product" button appears at the bottom and pulses softly. Clean app UI style. 12 seconds.
```

### CLIP 12 – Product Created Success
```
A green success toast notification slides in from the top right corner of the screen. It reads "✓ Product created successfully!" with a white checkmark. Below, a products table row smoothly appears: a small tomato image, the text "Fresh Organic Tomatoes", the price "£3.99", stock "50", and small Edit and Delete buttons. The toast fades out after a moment. UI feedback animation. 8 seconds.
```

### CLIP 13 – Bulk Actions
```
A products list table is shown with three rows of products. Checkboxes on the left of each row tick themselves on one by one with a satisfying click feel. A toolbar appears at the top with a "Bulk Actions" dropdown that opens to reveal three options: "Update Price", "Update Stock", "Delete Selected". The options are highlighted in green, gold, and red respectively. Clean UI interaction. 10 seconds.
```

### CLIP 14 – Orders Page
```
An orders list page loads with three order cards stacking in from the top. The first card shows "Order #1234" with a yellow "Pending" badge. The second shows "Order #1233" with a blue "Confirmed" badge. The third shows "Order #1232" with a green "Delivered" badge and a checkmark. Each card displays a customer name, item count, and total price. Modern card-based UI style. 10 seconds.
```

### CLIP 15 – Order Status Flow
```
A horizontal timeline animates across the screen showing order stages. Circles connected by lines appear one by one, each lighting up in sequence: "Pending" in yellow, "Confirmed" in blue, "Preparing" in orange, "Packed" in purple, "Out for Delivery" in indigo, "Delivered" in green with a checkmark. Each label appears beneath the circle as it lights up. Clean infographic animation style. 10 seconds.
```

### CLIP 16 – Order Details Modal
```
A white modal card slides up showing full order details. At the top: "Order #1234" in bold. A section shows the customer name and delivery address. A list of ordered items with quantities and prices appears. At the bottom, the current status is shown as "Pending" in a yellow badge, with a green "Update Status" button beside it. Soft shadow and clean card design. 10 seconds.
```

### CLIP 17 – Reports Page
```
A reports dashboard loads with a large green area chart filling in smoothly showing revenue over 30 days. A date range selector at the top shows "Last 30 Days" selected. A small summary row below the chart shows total revenue, total orders, and average order value in green stat boxes. A blue "Export Report" button sits in the top right. Professional analytics UI style. 10 seconds.
```

### CLIP 18 – Settings Page
```
A settings page loads with a clean form layout. Fields are shown: "Store Name" with editable text, "Description" textarea, "Category" dropdown, logo upload area with a current logo, and a phone number field. All fields have a light gray background and green focus borders. A green "Save Changes" button sits at the bottom. Calm, clean UI style. 10 seconds.
```

### CLIP 19 – Success Montage
```
A fast-paced montage of quick shots: a product being added to a page, an order card flipping from Pending to Delivered, a revenue chart line climbing upward, a green checkmark animation, and finally the Afrimercato logo centered on screen. Each shot lasts about 1-2 seconds. Upbeat, celebratory energy. Bright greens and golds. 8 seconds.
```

### CLIP 20 – Vendor Outro
```
The Afrimercato logo sits centered on a dark green background. Below it, the text "Start Selling Today" fades in white. Below that, "support@afrimercato.com" and "www.afrimercato.com" appear. A subtle green glow pulses behind the logo. Clean, professional closing card. 6 seconds.
```

---

## CUSTOMER VIDEO – 20 SORA PROMPTS

### CLIP 01 – Intro Animation
```
Fresh vegetables and fruits – tomatoes, peppers, spinach, mangoes, carrots – float gently onto the screen from different directions against a clean white background. They arrange themselves into a neat grid. The Afrimercato logo fades in over them in green. The text "Fresh. Local. Delivered." appears beneath in gold. Playful, warm, food-themed animation. 10 seconds.
```

### CLIP 02 – Welcome & Landing Page
```
A mobile app landing page animates into view. A hero banner shows "Fresh groceries delivered to your doorstep" in large white text over a warm background. A location pin icon pulses with the label "Delivering to: London". Below, a row of colorful store cards slides in horizontally. Two green buttons labeled "Browse Stores" and "Shop Now" appear. Modern app UI style. 10 seconds.
```

### CLIP 03 – Sign Up Form
```
A clean registration form card appears on a light gray background. Fields labeled "Full Name", "Email", and "Password" are visible. A green cursor moves down and clicks each field. "John Doe" types into the name field, "john@email.com" into email, and dots appear in the password field. A green "Sign Up" button at the bottom is clicked with a satisfying pulse. App UI walkthrough. 12 seconds.
```

### CLIP 04 – Stores Listing
```
A page titled "Stores Near You" loads with a row of store cards animating in. Each card shows a store icon, store name, category tag, a star rating, and a distance label like "0.5 mi". Stores shown: "Fresh Market", "Green Grocer", "Baker's Corner". A search bar at the top has placeholder text "Search stores...". A filter bar shows category pills. Clean grid layout, modern UI. 10 seconds.
```

### CLIP 05 – Entering a Store
```
The user taps on the "Fresh Market" store card. It smoothly expands into a full store page. A colorful cover image banner appears at the top with the store logo overlaid. Below, the store name "Fresh Market" in bold, a short description, and a star rating appear. A grid of product cards begins loading below with images, names, and prices. Smooth page transition animation. 12 seconds.
```

### CLIP 06 – Products Browsing Page
```
A products page loads showing a grid of product cards in two columns. Each card has a product image, name, price per unit, and a small star rating. A search bar sits at the top. Below the search bar, category filter pills show: "All", "Fresh Produce", "Dairy", "Bakery" – with "All" highlighted in green. A sort dropdown shows "Sort by: Popular". Clean e-commerce UI style. 10 seconds.
```

### CLIP 07 – Product Card Zoom
```
The camera smoothly zooms into a single product card. It shows a stack of fresh red tomatoes as the image. The name "Fresh Tomatoes" appears in bold below. The price "£3.99/kg" is shown in green. Four and a half gold stars indicate the rating. A green "Add to Cart" button sits at the bottom of the card. The card has a subtle shadow and rounded corners. Clean focus effect. 8 seconds.
```

### CLIP 08 – Product Detail Page
```
A full product detail page loads. A large tomato image is at the top with two more thumbnail images in a row below it. The product name "Fresh Tomatoes" is in bold. A paragraph description reads about the product. "Sold by Fresh Market" appears as a clickable link. Minus and Plus buttons flank a quantity number "1". A large green "Add to Cart" button is at the bottom. Stars and a review count are shown. Clean layout. 12 seconds.
```

### CLIP 09 – Add to Cart Animation
```
The "Add to Cart" button is clicked and pulses green. A small tomato image lifts off the product card and flies in a smooth arc toward the top-right corner of the screen where a shopping cart icon sits. When it reaches the cart, the cart icon jiggles slightly and a green badge appears showing "1". The button text changes to "✓ Added!". Fun, satisfying micro-animation. 6 seconds.
```

### CLIP 10 – Cart Badge Updates
```
The top navigation bar is shown. The cart icon sits on the right. Items are added off-screen and the badge number updates: 1, then 2, then 3 – each time with a small bounce animation on the number. The cart icon gently shakes each time. Simple, clean notification animation on a white nav bar. 5 seconds.
```

### CLIP 11 – Shopping Cart Page
```
A cart page loads with three item rows stacking in from the top. Each row shows a product thumbnail, name, price, and minus/plus quantity buttons on the right with a trash icon. Item totals are shown on the right side of each row. A divider line separates items from a "Subtotal: £12.46" line. A large green "Proceed to Checkout" button sits at the bottom. Clean list UI style. 12 seconds.
```

### CLIP 12 – Checkout Step 1 Address
```
A checkout page loads. A progress bar at the top shows three steps – "Address" is highlighted in green, the other two are gray. A form below shows fields: Full Name, Phone, Street Address, City, Postcode. Each field fills in with example data one by one – "John Doe", "+44 7123 456789", "123 Main Street", "London", "SW1A 1AA". An optional "Special Instructions" field at the bottom fills with "Ring doorbell twice". A green "Continue" button appears. 14 seconds.
```

### CLIP 13 – Checkout Step 2 Payment
```
The checkout page transitions to step 2. The progress bar now highlights "Payment" in green. The screen splits: on the left, a payment form with Card Number, Expiry, and CVC fields filling in with example data. A small lock icon with "Secure" text sits above. A "Save card for future" checkbox is shown. On the right, an order summary panel shows the three items, subtotal £12.46, delivery fee £5.00, and total £17.46 in bold green. 14 seconds.
```

### CLIP 14 – Checkout Step 3 Review
```
The checkout page transitions to step 3. The progress bar highlights "Review" in green. The page shows three summary sections: delivery address box, payment method box showing a card ending in 4242, and an items list box. At the very bottom, the total "£17.46" is displayed in large green text. A prominent green "Place Order" button pulses gently to draw attention. Clean review layout. 10 seconds.
```

### CLIP 15 – Order Confirmed
```
The screen transitions to a celebration. A large green checkmark animates in with a circular burst effect. Below it, "Order Confirmed!" appears in bold green text. The order number "Order #12345" fades in beneath. An estimated delivery time label appears. A green "Track Your Order" button slides up at the bottom. Confetti particles fall gently in the background. Joyful, celebratory mood. 8 seconds.
```

### CLIP 16 – Order Tracking Timeline
```
An order tracking page loads. On the left side, a vertical timeline is drawn from top to bottom. Five circles connected by a line appear one by one. Each is labeled: "Confirmed" with a green checkmark, "Preparing" with a green checkmark, "Packed" with a green checkmark, "Out for Delivery" with a glowing green circle and subtle pulse, "Delivered" as a gray empty circle. The current status "Out for Delivery" glows. 10 seconds.
```

### CLIP 17 – Live Delivery Map
```
A map view loads on the right side of the screen. It shows a simplified city street layout in light gray. A green pin marks "Your Location". A red scooter/bike icon labeled "Mike" moves slowly along a road toward the green pin. A small card below the map shows the rider's name, a smiley avatar, and estimated arrival time. The scooter continues to move. Real-time tracking feel. 10 seconds.
```

### CLIP 18 – Order Delivered
```
The tracking timeline animates to completion. All five circles light up green with checkmarks. The last circle "Delivered" fills green and a checkmark draws itself. A banner at the top reads "Your order has been delivered!" in green on white. Below, two buttons appear: "Rate Your Experience" with gold stars, and "Reorder" with a refresh icon. Warm, satisfying completion animation. 8 seconds.
```

### CLIP 19 – Extra Features Quick Show
```
A fast montage of three screens. First: a Wishlist page showing saved products in a grid with red heart icons. Second: an Order History page showing a clean list of past orders each with a "Reorder" button. Third: a Notifications panel sliding in from the right showing order updates and a promo notification. Each screen appears for about 2 seconds. Quick, clean transitions. 8 seconds.
```

### CLIP 20 – Customer Outro
```
Fresh vegetables float gently around the edges of the screen. The Afrimercato logo fades in at the center in green. Below it: "Start Shopping Today!" in bold white text. Below that: "www.afrimercato.com" in lighter text. A warm green glow pulses softly behind the logo. Friendly, inviting closing screen. 6 seconds.
```

---

## QUICK REFERENCE: SORA SETTINGS

```
Style keywords to include in every prompt:
  - "app UI mockup style"
  - "animated walkthrough"
  - "clean, modern interface"
  - "smooth animation"

Colors to mention:
  - Green (#22C55E) for buttons and highlights
  - Gold (#EAB308) for accents and ratings
  - White cards, light gray backgrounds

After generating all clips:
  1. Download all 40 clips (20 vendor + 20 customer)
  2. Open CapCut or any editor
  3. Arrange clips in order
  4. Paste ElevenLabs voiceover from 05-AI-PROMPTS.md
  5. Sync audio to clip transitions
  6. Add background music at 10-15% volume
  7. Export as 1080p MP4
```

---

## TOTAL CLIPS NEEDED

| Video | Clips | Approx. Total Length |
|-------|-------|---------------------|
| Vendor | 20 | 4 min 15 sec |
| Customer | 20 | 4 min 30 sec |
| **Total** | **40** | **~9 min combined** |

